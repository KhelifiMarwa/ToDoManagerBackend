<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ToDoManagerMobile.Views.HomePage"
             BackgroundColor="#F5F5F5">
    <VerticalStackLayout Padding="20">
        <Label Text="🏠 Welcome, Marwa!" FontSize="24"/>
      
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="20">

                <!-- Welcome --><!--
                <Label Text="Welcome, Marwa!"
                   FontSize="24"
                   FontAttributes="Bold"
                   TextColor="#212121"/>-->

                <!-- Tasks summary -->
                <Grid ColumnDefinitions="*,*,*"
                  RowDefinitions="Auto"
                  BackgroundColor="White"
                  Padding="15">

                    <StackLayout HorizontalOptions="Center">
                        <Label Text="Total Tasks" FontSize="14" TextColor="#757575"/>
                        <Label Text="12" FontSize="18" HorizontalOptions="Center" FontAttributes="Bold" TextColor="#2196F3"/>
                    </StackLayout>

                    <StackLayout HorizontalOptions="Center" Grid.Column="1">
                        <Label Text="Completed" FontSize="14" TextColor="#757575"/>
                        <Label Text="5" FontSize="18" HorizontalOptions="Center" FontAttributes="Bold" TextColor="#4CAF50"/>
                    </StackLayout>

                    <StackLayout HorizontalOptions="Center" Grid.Column="2">
                        <Label Text="Pending" FontSize="14" TextColor="#757575"/>
                        <Label Text="7" FontSize="18" HorizontalOptions="Center" FontAttributes="Bold" TextColor="#F44336"/>
                    </StackLayout>
                </Grid>

                <!-- Quick actions -->
                <HorizontalStackLayout Spacing="15" HorizontalOptions="Center">
                    <Button Text="Add Task" BackgroundColor="#2196F3" TextColor="White" CornerRadius="20" WidthRequest="120"/>
                    <Button Text="View Tasks" BackgroundColor="#4CAF50" TextColor="White" CornerRadius="20" WidthRequest="120"/>
                </HorizontalStackLayout>

                <!-- Recent tasks preview -->
                <Label Text="Recent Tasks" FontSize="18" FontAttributes="Bold" TextColor="#212121"/>

                <CollectionView x:Name="RecentTasksCollection"
                            ItemsLayout="VerticalList"
                            HeightRequest="200">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Frame CornerRadius="10" Padding="10" Margin="5" BackgroundColor="White" HasShadow="True">
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="{Binding Title}" FontSize="16" FontAttributes="Bold"/>
                                    <Label Text="{Binding DueDate, StringFormat='{0:MM/dd/yyyy}'}" FontSize="12" TextColor="#2196F3"/>
                                </Grid>
                            </Frame>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>

            </VerticalStackLayout>
        </ScrollView>
    </VerticalStackLayout>
</ContentPage>